node_modules/
.env
.env.*
!.env.example
dist/
out/
.DS_Store

# Session signers and other local secrets. Never commit.
.secrets/

# Next.js build output
.next/
next-env.d.ts

# Vercel
.vercel/

# Agent tooling installed into the repo, not part of the project.
# `bag skills install` (BNB Agent Studio CLI) writes 16 skill documents plus a
# .bag-meta.json here. Useful locally, noise in a submission.
.claude/

*.tsbuildinfo
