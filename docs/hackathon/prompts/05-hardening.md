Continue in the AgenSea repo (apps/web). Three fixes, one branch feat/hardening. Save this prompt verbatim to docs/hackathon/prompts/05-hardening.md first and commit "docs: save prompt 05 (hardening)". LLM budget: Groq free tier is 200k tokens/day; you may run at most TWO live Scout questions in this whole task, both at the end in STEP 4. Everything else must be verified with unit tests or mocked streams.

FIX 1. Duplicate submission and duplicate rendering on /scout. In production, clicking an example chip and then ASK SCOUT produced two verdict cards for the same agent and two answer paragraphs concatenated. Required behaviour:
  - Clicking a chip fills the input only. It does not submit.
  - ASK SCOUT and Enter submit. While a request is streaming, the button is disabled, the input is read-only, chips are disabled, and a second submit is impossible. Abort any in-flight request on unmount.
  - Each submission replaces the previous answer, cards, and evidence panel (single-answer view), it does not append.
  - Client-side dedupe: render at most one card per agentId per answer, regardless of what the stream contains. Server-side: confirm the existing duplicate-verdict guard is actually applied to what gets streamed to the client, not only to the tool result.
  - Answer text: ensure text parts are joined with proper whitespace so two model text segments never fuse into one word.
  Write a component test or a mocked-stream test that proves a stream containing two emit_verdict calls for the same agent renders one card.

FIX 2. Provider fallback. In lib/llm.ts and the /api/scout route: when the primary provider (LLM_PROVIDER, default groq) returns a 429 whose body indicates a daily quota (tokens per day or requests per day exhausted, detect by matching "per day" or "TPD" or "RPD" case-insensitively in the error body), retry the whole request once on LLM_FALLBACK_PROVIDER (default google). Do not fall back on per-minute 429s, keep the existing short backoff for those. The "answered by <model>" line must show the model that actually answered. Add LLM_FALLBACK_PROVIDER to .env.example. Unit test the classifier that decides daily-quota vs per-minute with sample error bodies from both Groq and Google formats (construct plausible bodies, note in the test that they are synthetic).

FIX 3. Footer "last job: unavailable". Root cause found earlier: lib/server/last-job.ts walks down from the on-chain job counter with MAX_WALK 25, but our newest recorded demo hire is ~70 jobs behind the counter now. Replace the walk with a direct lookup: query Supabase demo_deliverables for the newest job belonging to our provider (highest job id), then fetch exactly that one job on-chain with the existing getErc8183Job and run the existing hash verification unchanged. Keep the walk only as a fallback when Supabase returns nothing. Do not change the rendered strip's format or the verification logic. Verify locally that the strip now shows a real job.

STEP 4. Run typecheck and all tests. Commit per fix (three commits). Merge into main with --no-ff, push main and the branch, wait for the Vercel auto-deploy to be Ready. Then run exactly two live checks against production: (1) POST /api/scout with "Is 56:30867 safe to hire?" and confirm the stream contains exactly one recorded verdict for 56:30867 and one final text answer; (2) fetch the homepage and confirm the footer shows a real last job, not "unavailable". Also confirm that the primary provider was used, not the fallback, for check 1.

STEP 5. Report: (a) what the double-submission cause actually was, (b) the fallback classifier test cases, (c) the last job now shown in the footer, (d) results of the two live checks, (e) git log --oneline -5 on main.
