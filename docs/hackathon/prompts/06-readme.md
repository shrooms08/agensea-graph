Continue in the AgenSea repo. Write the submission documentation for ETHOnline 2026. Branch docs/hackathon-readme. Save this prompt verbatim to docs/hackathon/prompts/06-readme.md and commit first.

CONTEXT YOU MUST USE. Hackathon: ETHOnline 2026 (ETHGlobal), Continuity pool ("Extend Open Source"). Partner tracks applied for: The Graph "Best AI Tooling or AI Use Case, Continuity" and The Graph "Best Use of Composable or Standardized Graph Products", plus Hedera (read hedera/ to see what was built and describe it accurately; if unsure of any claim, say "see hedera/README" rather than guessing). Pre-existing work is everything at git tag pre-hackathon-baseline (commit e0d1392). All hackathon work is main after that tag. Judging criteria: technicality, originality, practicality, usability, wow factor. The Graph requirements: live data from a Graph provider, meaningful reasoning over it, open source, runnable README.

STEP 1. Gather facts, do not invent: git log pre-hackathon-baseline..main --oneline, git diff --stat pre-hackathon-baseline..main, the list of prompts in docs/hackathon/prompts/, the five subgraph IDs from lib/graph/chains.ts, the cached adoption numbers from the live site (curl https://agensea-graph.vercel.app/api/graph/adoption), the test count, and what hedera/ contains.

STEP 2. Rewrite the root README.md (keep any existing AgenSea product intro if present, but it must be under 15 lines and clearly labelled as the pre-existing product). Required sections, in this order:
  1. One-paragraph pitch: AgenSea's Trust Layer, powered by The Graph's Agent0 subgraphs. Live link https://agensea-graph.vercel.app, direct links to /scout and /adoption.
  2. "What was built during ETHOnline" with a bullet per feature: Graph data layer (lib/graph), Scout trust analyst (/scout, /api/scout, lib/scout), cross-chain adoption page (/adoption, /api/graph/adoption), provider failover, hardening, the Hedera work. Each bullet names the files and links the commit range.
  3. "What existed before" pointing at the tag and summarizing the pre-existing AgenSea in 5 bullets (BSC marketplace, own ERC-8004 sweep via Multicall3, Supabase data layer, four first-party agents, ERC-8183 hiring). State plainly that only post-tag work is submitted for judging.
  4. "How The Graph is used" with a table: chain, subgraph ID, what is queried. Then 4 bullets on why it is load-bearing: Scout's every claim comes from subgraph queries shown in the Evidence panel; adoption page is a single query pattern run across five deployments of one shared schema; nested filters give search across all 345k BSC agents; independent cross-check of AgenSea's own 1.35% measurement against The Graph's 1.3%.
  5. "Key findings from the data" with the live adoption numbers per chain, the cumulative-counter gotcha, Feedback.value units per tag, validation registries absent on all live chains, Monad indexer outage handled.
  6. "Architecture" as a short ASCII diagram: browser → /scout page → /api/scout (AI SDK tool loop) → lib/graph (gateway) → Agent0 subgraphs; and the cached adoption path.
  7. "Run it locally" with exact commands: clone, npm install at the right directories, the env vars needed (from .env.example), npm run dev:env, and the three URLs to open. Include npm run graph:smoke and npm test.
  8. "AI usage disclosure": Claude (claude.ai) acted as tech lead writing plans and prompts; Claude Code executed all coding from those prompts, which are saved verbatim in docs/hackathon/prompts/ in order; Minos directed product decisions, ran all deploys and tests, and reviewed outputs; runtime LLM for Scout is Groq gpt-oss-120b with Gemini failover. Be factual, not defensive.
  9. "Known limitations": Groq free-tier daily token cap and what happens when it's hit, Monad indexer, validation registries, NAME_LOOKALIKE is heuristic, Scout answers can take 5 to 50 seconds.
  10. "Demo video" placeholder line to be filled with the link.

STEP 3. Create docs/hackathon/SUBMISSION.md containing draft text for the ETHGlobal submission form: project name, a 2-sentence tagline, a 250-word description, a "how it's made" section (300 words, technical, mention the Vercel AI SDK, unstable_cache, nested subgraph filters, cumulative aggregations, the failover), and per-partner blurbs: The Graph (which subgraphs, which tracks, what feedback we have for them: the 20-page cap trap on counting, cumulative counters being easy to misread, Monad indexer availability), and Hedera (from hedera/). Keep every claim consistent with README.

STEP 4. Commit, merge into main with --no-ff, push. Confirm the README renders on GitHub by fetching https://raw.githubusercontent.com/shrooms08/agensea-graph/main/README.md and checking the section headings are present.

STEP 5. Report: (a) the commit count and files changed since the tag, (b) the README section list as rendered, (c) the two-sentence tagline you wrote, (d) what you described hedera/ as, in two lines, so Minos can correct it.
